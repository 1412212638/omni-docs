interface:
  display_name: "OmniRouters"
  short_description: "Manage OmniRouters models, tokens, balance, and request usage."
  default_prompt: "Use $omnirouters to inspect OmniRouters models, tokens, balance, or a request usage record."

policy:
  allow_implicit_invocation: true
